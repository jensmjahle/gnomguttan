# Runtime environment variables (set in docker-compose.yml, not .env)
# These are injected at container start via docker/entrypoint.sh

# Your self-hosted VoceChat instance URL (no trailing slash)
VOCECHAT_HOST=https://chat.gnomguttan.no

# App display title
APP_TITLE=Gnomguttan
